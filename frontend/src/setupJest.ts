// Setup for Jest environment
import '@testing-library/jest-dom';

// Note: For testing the authentication service, we'll use direct URL mocking in the tests
// rather than trying to mock the Vite import.meta.env, which is challenging in Jest
